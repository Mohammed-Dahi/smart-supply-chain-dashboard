## Cleaned Data

The cleaned dataset is generated by running the data processing script.

To reproduce it, run the notebooks in `3_Python_EDA/` against the raw data
from `1_Raw_Data/` — the cleaned CSV will be exported here automatically.
